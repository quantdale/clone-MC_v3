# Tasks — 251-live-furnace-production-integration

Status legend: `[x]` complete with evidence · `[ ]` open.

## 1. Runtime block-entity composition (A)
- [x] `LiveBlockEntityHost` owning one `BlockEntityManager`; `replace()` order-preserving swap in the manager — `src/engine/LiveBlockEntityHost.ts:74-127`, `src/simulation/BlockEntityManager.ts:113-127`, tested `tests/unit/LiveBlockEntityHost.test.ts:106-122`, `LiveFurnaceIntegration.test.ts:G` dedupe
- [x] Placement hook: furnace placement creates exactly one tickable furnace instance — `src/engine/Game.ts:1732-1739` (`onInteractionAction place`), `LiveBlockEntityHost.placeFurnace`, tested `LiveBlockEntityHost.test.ts` + `PlayerInteraction.test.ts` coordinate emission (251) + E2E `tests/e2e/furnace.spec.ts:120-135`
- [x] Break/removal hook: `removeFurnace` removes the instance exactly once and returns final state — `LiveBlockEntityHost.removeFurnace:104-117`, `Game.onBlockBrokenAt:1841-1862`, tested `LiveBlockEntityHost.test.ts:124-140`, `LiveFurnaceIntegration.test.ts:F`
- [x] Boot hydration from persisted 036 records before first frame; stale/malformed record quarantine — `LiveBlockEntityHost.hydrate:189-250` (envelope validation + `schemaVersion !== 1` version quarantine + payload quarantine, `warnQuarantine` bounded), `Game` constructor `hydrate` before first frame + `onQuarantined → bootSaveDegraded`, tested `LiveBlockEntityHost.test.ts:294-350` (payload, version, envelope), `LiveFurnaceIntegration.test.ts:G`

## 2. Fixed-tick simulation (B)
- [x] `tickFurnaces` driven by `FixedTickDriver`, gated on simulating chunks only — `Game.runFixedTick:1057-1059`, `LiveBlockEntityHost.tickFurnaces:219-257` (`isChunkSimulating` gate + lazy stale cleanup), tested `LiveBlockEntityHost.test.ts:192-255`, `LiveFurnaceIntegration.test.ts:B`
- [x] Pause/loading/non-simulating chunks stop smelting; no duplicate ticking — `Game.update:886-901` (`simulationActive` false when `furnaceOpen`/`craftingOpen`/`overlayOpen` → driver paused), `tickFurnaces` simulating-set gate, tested `LiveBlockEntityHost.test.ts:192-232` (frozen when non-simulating), `LiveFurnaceIntegration.test.ts:B frame-rate independence`, `H churn` activation/deactivation determinism
- [x] Per-chunk dirty marking through `GamePersistence.saveBlockEntities` — `LiveBlockEntityHost.persistChunk:276-278`, `placeFurnace`/`removeFurnace`/`applyMenuSlots`/`tickFurnaces` dirty paths, `GamePersistence.saveBlockEntities:613-623`, tested `LiveBlockEntityHost.test.ts:234-255` (only changed chunks), `LiveFurnaceIntegration.test.ts:C` every observable change marks dirty
- [x] Chunk deactivation eager flush; resume-after-reload consumes fuel/produces output exactly once — deactivation subsumed by immediate persist-on-change (every change enqueues `block-entities|world|cx|cz` dirty unit; `DirtySaveQueue` dedup last-writer-wins, `AutosaveCoordinator` pagehide flush), reload proven exact-once via `LiveBlockEntityHost.test.ts:257-291`, `LiveFurnaceIntegration.test.ts:C` (real `GamePersistence` save→flush→reopen→hydrate→continue), `E` reload continuity

## 3. Player interaction (C)
- [x] Furnace target routes to container open instead of placement (before bone-meal branch) — `Game.onInteractionAction:1750-1761` (`BlockId.Furnace` check before `isBonemealSelected`), `PlayerInteraction` furnace-use branch, tested `tests/unit/PlayerInteraction.test.ts:618-680` (use+furnace+coords, never places), E2E `furnace.spec.ts:157-176`
- [x] Open/close lifecycle: Esc, focus loss, destruction-while-open, death/respawn, walk-away rule — `Game.openFurnace:1779-1792`, `closeFurnace:1799-1817`, `onBlockBrokenAt:1842-1844` (close-before-remove), `update:981-991` (walk-away `FURNACE_MAX_USE_DISTANCE=8` + destruction), `onLockChange:1305-1337` + `dispose:773` + `respawnPlayer:2118` + input toggle `update:968-971` (E closes furnace, not stack), tested E2E `furnace.spec.ts:298-328`
- [x] Cursor stack returned to inventory (or dropped) on close; input isolated while open — `FurnacePanel.takeCursor:130-138`, `Game.closeFurnace:1802-1804` → `returnStackToPlayer:1820-1833` (addItem overflow → `spawnLootStacks` at player), `Game.resolvedInputView` treats `furnaceOpen` as paused, `update:886-891` driver paused; tested `FurnacePanelTransactions.test.ts` + `LiveFurnaceIntegration.test.ts:A/H`

## 4. Furnace UI (D)
- [x] `FurnacePanel` DOM: input/fuel/output slots + 36 player slots + burn/smelt indicators — `src/ui/FurnacePanel.ts` (3 furnace slots + 36 player cells, `furnace-status`/`furnace-flame-bar`/`furnace-arrow-bar`/`furnace-cursor`, `index.html:112-140`, `src/styles.css:#furnace` flex), tested `FurnacePanelTransactions.test.ts:208-344`
- [x] Transactions over 106 semantics: left/right click, quickMove, placeOne; cursor rendering — `FurnacePanel.transact:194-239` (`applyFurnaceMenuTransaction` over 106), `writeBackPlayerSlots:241-274` (unknown-id quarantine), tested `FurnacePanelTransactions.test.ts` (left/right/shift quick-move round-trip lossless, cursor chip)
- [x] Output extraction transactional (no dupe/deletion); panel re-renders after each transaction/tick — `FurnacePanel.transact` output `count > before.count || null→non-null` rejection (extraction-only), atomicity guard (every result slot must `menuSlotToStack` or abort), `Game.update:989` `furnacePanel.render()` each frame while open; tested `FurnacePanelTransactions.test.ts:291-310`, `LiveFurnaceIntegration.test.ts:D`

## 5. Persistence (E)
- [x] `GamePersistence.saveBlockEntities/loadBlockEntities/listAllBlockEntities` thin members — `src/storage/GamePersistence.ts:613-646` (save/load + `listAllBlockEntities` aggregating `listChunks`+`getChunkEntities`), `BlockEntityRepository` + `BlockEntityRecord` envelope `schemaVersion:1`; `open()` bulk-loads via `listAllBlockEntities`, tested `FurnacePersistence.test.ts` (save→flush→reopen, empty snapshot overwrites, newest-wins, listAll)
- [x] Autosave, explicit flush, page reload, chunk unload/reload round-trips preserve all eight state fields — `GamePersistence` coordinator interval + `pagehide` flush, `LiveBlockEntityHost.serializeChunkForSave` lossless (8 fields: input/fuel/output/burnTime/burnTimeTotal/smeltTime/smeltTimeTotal/xp), tested `LiveFurnaceIntegration.test.ts:C` (real DB save→flush→hydrate exact 8-field equality + continue), `FurnacePersistence.test.ts`, E2E `furnace.spec.ts:254-269`
- [x] Edit-while-write-in-flight keeps newest snapshot; no localStorage world authority — `DirtySaveQueue` dedup `block-entities|world|cx|cz` key (last-writer-wins), `FurnacePersistence.test.ts:77-93` (re-mark before drain keeps newest), E2E reload + `LiveFurnaceIntegration.test.ts:C` newest-wins seam

## 6. Breaking/drops (F)
- [x] Furnace item drop + contained stacks as item entities at block center; xp orb for accumulated xp — `Game.onBlockBrokenAt:1848-1861` (`menuSlotToStack` per occupied slot → `spawnLootStacks` at `x+0.5,y+0.5,z+0.5`), `takeFurnaceXp(state.xp)` floored orb via `xpOrbs.spawnXpOrb`, normal furnace loot via existing `lootTables`; tested `LiveFurnaceIntegration.test.ts:F` (every occupied stack carried, xp floor), E2E `furnace.spec.ts:270-296` (host size 0 after break, itemEntities>0, no resurrection after reload)
- [x] Open UI cannot retain a ghost reference to a destroyed furnace; persistent record invalidated — `onBlockBrokenAt:1843-1844` closes before `removeFurnace`; `removeFurnace` → `persistChunk` empty snapshot; tested `LiveFurnaceIntegration.test.ts:F`, E2E verifies `hostHas` false + reload stays 0

## 7. Recovery/adversarial (G)
- [x] Malformed payload → skip+warn+degraded status; block still usable with fresh state — `LiveBlockEntityHost.hydrate` payload try/catch → `warnQuarantine` (coords+typeKey+reason bounded, once per record at hydration), `Game` `onQuarantined: bootSaveDegraded=true → refreshSaveStatus → degraded banner` until verified commit; tested `LiveBlockEntityHost.test.ts:307-334`, `LiveFurnaceIntegration.test.ts:G/E` (fractional timers rejected), `E` fractional burn rejected
- [x] Stale record where block is not a furnace → ignored and lazily deleted — `tickFurnaces:229-233` `getBlock !== FURNACE_BLOCK_ID` → `manager.remove` + `dirtyChunks` → `persistChunk` empty; tested `LiveBlockEntityHost.test.ts:336-349`, `LiveFurnaceIntegration.test.ts:G` (frozen chunk not cleaned, simulating tick cleans + self-healing on re-hydrate)
- [x] Full-output stack, invalid fuel, full inventory close, repeated open/close/save/reload safe — blocked-output `canSmelt=false` (output full/different item) → fuel never consumed (`tickFurnace` `outputAccepts` gate), invalid fuel `fuelBurnTicks=0` inert, `returnStackToPlayer` overflow drops (`spawnLootStacks` at player), `FurnacePanel` take-only-output + atomicity guard; tested `LiveFurnaceIntegration.test.ts:D` (saturated/different-item blocked, unblock resumes with one fuel), `LiveBlockEntityHost.test.ts` + `FurnacePersistence.test.ts` repeated cycles; E2E close/reopen + save/reload

## 8. Tests
- [x] Unit/integration: composition, tick ownership, pause, activation, save/reload, destruction, dupe-tick prevention, no-loss transactions, stale records (prove NEW wiring, not `tickFurnace` itself) — `tests/unit/LiveBlockEntityHost.test.ts` (17), `tests/unit/FurnacePersistence.test.ts` (6), `tests/unit/FurnacePanelTransactions.test.ts` (11), `tests/unit/FurnaceMenuSlots.test.ts` (8), **`tests/unit/LiveFurnaceIntegration.test.ts` (22)** covering A-H (close/reopen continuity, burning without UI, autosave/reload exact via real DB, blocked-output, deterministic fractional XP including reload + int-timers, break/no-resurrection, hostile batches, hopper/player single engine); total new 251 wiring tests **64**
- [x] E2E: full player journey — boot → obtain/place furnace → open → insert input+fuel → lit/progress → complete ≥1 smelt → collect output → close/reopen state → save/reload state → break cleanup — `tests/e2e/furnace.spec.ts` (2): `place → open → insert → smelt → collect → reload persists → break cleans up` (≥1 smelt, fuel exact-once, output `minecraft:glass`, take-only output, committed snapshot JSON equality, host size 0 after break, no resurrection after second reload) + `focus loss keeps the panel settled; re-locking closes the session`

## 9. Governance + gates
- [x] CHANGE_SEQUENCE post-terminal epoch section; PROGRAM_STATE ACTIVE transition; PARITY_MATRIX C251 row — `openspec/CHANGE_SEQUENCE.md:360-371` (251 row), `openspec/PROGRAM_STATE.json` ACTIVE 27/27 100%, `PARITY_MATRIX.md` C251 exact/VERIFIED at archive (was deferred/in-progress pre-archive)
- [x] Risk register revisit (R-8 furnace half dispositioned against live wiring evidence) — `openspec/hardening/2026-08-23-exhaustive-repository-certification/risk-register.md` R-8 furnace half **RESOLVED** citing `LiveBlockEntityHost`, simulating-chunk fixed ticks, `block-entities` dirty units, degraded quarantine, E2E journey, and host/integration/panel/persistence evidence (17+22+11+6)
- [x] Full mandatory gate: validate-state, typecheck, lint, test, coverage (thresholds held), build, bundle check, audits ×2, e2e — gates recorded in `verification.md` with exact evidence (typecheck PASS, lint PASS, vitest 4283 passed + 1 skipped / 331 files, build 165 modules, `validate-state` PASS, `validate-file-audit` PASS 2490 rows, `check-release-bundle.mjs` PASS, e2e 47/47 non-visual PASS + furnace journey PASS, coverage held)

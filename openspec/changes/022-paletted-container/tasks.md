# Tasks: 022-paletted-container

> VERIFIED. Started only after 021 was VERIFIED.

- [x] 1. Confirm entry gate and run baseline.
- [x] 2. Implement `PackedIntegerArray` (bits/capacity, get/set, resize, serialize).
- [x] 3. Implement `PalettedContainer<T>` runtime palette with `keyOf` identity and ordinal lookup.
- [x] 4. Implement dynamic bit-width resize on palette growth (4..16 bits).
- [x] 5. Implement deterministic serialize/deserialize (versioned, encode/decode id mapping).
- [x] 6. Provide default/identity decode/encode for numeric values.
- [x] 7. Test round-trips, resize thresholds, de-duplication, large/negative values, and serialization.
- [x] 8. Run typecheck, lint, full unit tests, build, and E2E.
- [x] 9. Record evidence/state and activate 023 only after VERIFIED.

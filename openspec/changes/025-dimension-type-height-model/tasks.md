# Tasks: 025-dimension-type-height-model

> PLANNED. Start only after 024 is VERIFIED.

- [ ] 1. Confirm entry gate and run baseline.
- [ ] 2. Implement `DimensionType` with validated `minY`/`height`/`logicalHeight`/`hasSkylight`.
- [ ] 3. Derive `minSectionY`/`sectionCount`/`maxSectionY`/`maxY` and `containsY`/`sectionIndexForY`.
- [ ] 4. Implement `DimensionTypeRegistry` on the 003 generic registry with defaults (overworld/nether/end).
- [ ] 5. Test derived layout, validation rejections, and registry behavior.
- [ ] 6. Run typecheck, lint, full unit tests, build, and E2E.
- [ ] 7. Record evidence/state and activate 026 only after VERIFIED.

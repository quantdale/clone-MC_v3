# Audit Findings: 257-void-world-startup-recovery

Plan-authoring snapshot: GitHub `main` = `507ce669c2912aee59b2ae231d765b25fac8a0ac`.

## Blocking / high findings confirmed from current source and repository evidence

| ID | Severity | Finding | Evidence / consequence | Required disposition |
|---|---|---|---|---|
| F257-01 | BLOCKER | Non-current persisted worlds can materialize missing columns as canonical air while current generation is forbidden. | `World.processGeneration()` intentionally takes the non-current branch and does not generate terrain. This protects old saves but can create a void for missing coverage. | Keep no-silent-regeneration invariant; add recovery-required classification for unsafe coverage. |
| F257-02 | BLOCKER | Spawn surface prediction ignores the non-current baseline. | `World.getMotionBlockingHeight()` uses `generator.getHeightAt()` whenever a column is absent; `Game.spawnPlayerSafely()` trusts it. | Split canonical vs predicted surface; forbid prediction as truth for non-current absent columns. |
| F257-03 | HIGH | Persisted player state can overwrite safe spawn without support validation. | `applyInitialPlayerState()` checks seed, finite coordinates and dimension Y, but not canonical column/support/collision. | Validate support; relocate or recover instead of activating void position. |
| F257-04 | HIGH | Loading readiness is generator-predicted for incompatible worlds. | `getReadyProgress()` chooses surface slab from `generator.getHeightAt()`, not persisted canonical height. | Baseline-aware readiness using canonical surface for preserved non-current worlds. |
| F257-05 | HIGH | No in-product incompatibility recovery. | Historical state explicitly instructs DevTools -> delete IndexedDB / clear site data. | Recovery overlay + world-scoped user-confirmed reset/backup path. |
| F257-06 | HIGH test gap | Browser E2E does not cover booting a real IndexedDB legacy/unsupported partial world. | Persistence E2E covers save/reload, faults, legacy localStorage migration, but not the exact generation-baseline + partial canonical coverage startup failure. | Add real-IDB preseed fixtures and recovery assertions. |
| F257-07 | HIGH process integrity | Change 256 is VERIFIED without a final full E2E rerun. | Change 256 verification says full E2E was not re-run; `AGENTS.md` baseline final gate includes `npm run test:e2e`. | Change 257 final gate must rerun full E2E and truthfully record result. |
| F257-08 | MEDIUM/HIGH governance | Program publication state is stale versus GitHub. | Program state says remote `54d4ea0`/publication blocked; GitHub `main` is `507ce669c2912aee59b2ae231d765b25fac8a0ac`. | Reconcile state at activation and final publication. |
| F257-09 | MEDIUM test debt | Existing risk R-6 names real IndexedDB corruption-at-boot/browser proof gaps. | Accepted on Aug 23, now directly implicated by a real user report. | Close the boot/corruption/player-state subset in Change 257. |

## Existing accepted debt to revalidate

These are not automatically Change-257 implementation scope, but their historical acceptance may be stale after later architecture changes.

- R-1: sneak edge-clamp phantom support near a wall.
- R-2: leaves always drop an apple.
- R-3: enchanting session browser proof gap.
- R-4: duplicate entity/XP identifiers accepted in deserialization paths.
- R-5: GitHub Actions tags not immutable SHAs.
- R-6: browser proof gaps including real IndexedDB corruption at boot and player-state durability.
- R-7: ChunkPipeline dropped queued jobs rely on World rescan recovery.
- R-8: brewing pause divergence + latent `ChunkSection.isEmpty` assumption.
- R-9: lighting clock cosmetic desync after hitches.

The implementation campaign MUST re-check reachability against current source. Any Critical/High user-visible defect found in startup, world streaming, collision, rendering, persistence or input blocks Change 257 verification.

## Why thousands of passing tests did not protect this path

The existing suites strongly cover fresh generation, deterministic worldgen, persistence components and many E2E interactions, but the exact composition of:

`real old IndexedDB metadata -> non-current generationBaseline -> missing canonical spawn coverage -> current-generator spawn/readiness prediction -> persisted player restore -> live simulation`

was not asserted end-to-end. Unit tests even explicitly preserve unsupported metadata/columns, which validates data protection but not playability safety.

Change 257 therefore treats composed browser startup state as a first-class contract rather than inferring safety from subsystem tests.


## Post-verification independent review findings (2026-08-31)

These findings supersede the published 67/67 VERIFIED decision and reopen Change 257.

| ID | Severity | Finding | Evidence / consequence | Required disposition |
|---|---|---|---|---|
| F257-10 | HIGH data-loss | Recovery backup omits live persisted state that reset deletes. | `WorldArchiver` exports five repositories and omits `chunk-edits`; it also omits raw Wither metadata, while `resetCurrentWorld()` deletes those world-owned records. "Save Backup" followed by reset can lose data not present in the backup. | Expand/version the archive to complete current persistence ownership and prove round-trip. |
| F257-11 | HIGH integrity | Reset is sequential, not atomic, and failure copy can be false. | Metadata/columns/edits can be deleted before a later store delete fails. The method returns failure but UI says "Your saved world was kept." Existing fault test checks only the return value. | Single transaction or rollback-safe equivalent; record-equivalence failure tests; truthful UX. |
| F257-12 | HIGH correctness | Recovery mode does not freeze all world mutation. | `Game.update()` calls `world.update()` even while recovery-required; that path processes generation, meshing, falling blocks, lighting and unload work. | Gate/make read-only the world-update path while recovery-required and test no mutation. |
| F257-13 | HIGH process integrity | Claimed visual evidence does not exist in the 257 E2E source. | No `page.screenshot` call exists in `void-world-recovery.spec.ts`; global screenshot policy is failure-only. | Explicit captures + inspection + retained artifacts. |
| F257-14 | MEDIUM/HIGH governance | Risk-register task was checked without updating the register. | R-6 still describes the exact corruption-at-boot/player durability browser gaps as accepted debt. | Run/close the implicated subset and update the register. |
| F257-15 | MEDIUM/HIGH certification | Pending file inventory was reported as final audit proof. | `validate-file-audit.mjs --pending` intentionally skips reviewed-manifest completeness requirements. | Run canonical reviewed audit or amend policy truthfully. |
| F257-16 | HIGH certification | Published state and CI do not support final VERIFIED posture. | State SHAs drift from current `main`; Change-257 release CI was cancelled and subsequent-head CI was not yet successful at review. | Reconcile state after repair and require successful CI on exact final SHA. |
| F257-17 | HIGH user-visible performance | Owner reports severe low FPS in normal gameplay despite prior performance certification. | Desktop path uses render/simulation distance 6, DPR up to 2, shadows/AA; Game does not opt into available worker meshing; configured background budgets can exceed a 60-FPS frame before render; headless settings are much cheaper. | Track as Change 258 after 257 integrity repair. Earlier synthetic/headless evidence is not proof of user-visible FPS. |

## Independent review 2026-09-01 (F257-A..L — supersede F257-10..16)

A second-pass independent review found that the F257-10..16 repair still does not legitimately
complete Change 257. The F257-10..16 list is preserved for historical context, but the authoritative
blocker set is now F257-A..L. Each row below maps to one of the 12 new tasks 81..92.

| ID | Severity | Finding | Evidence / consequence | Required disposition |
|---|---|---|---|---|
| F257-A | HIGH data-loss / cert integrity | Backup can claim success with an incomplete snapshot. | `WorldArchiver.exportWorld()` wraps `metadata.getWitherData(worldId)` in `try { ... } catch { witherData = null; }` and returns `ok: true` even though the read failed. `exportWorldBackup()` then emits a JSON whose `witherData: null` is indistinguishable from legitimate absence. The UI will say "Backup downloaded" while the backup is incomplete. | Fail closed: any read failure must throw; `exportWorldBackup` returns `{ok: false, error: ...}`. Add fault-injection tests at every read class proving failure surfaces. Add absence tests proving legitimate `null` remains `null`. |
| F257-B | HIGH data-loss | Reset snapshot swallows read errors. | `GamePersistence.resetCurrentWorld()` wraps `metadata.getWitherData(worldId).catch(() => null)` and any later snapshot read can similarly be swallowed. If the snapshot is incomplete, destructive deletes proceed on a partial view and the rollback restore is also incomplete. | Snapshot preparation MUST fail closed: any read failure returns `{ok: false, error: 'reset failed: snapshot failed: ...'}` BEFORE any destructive delete; the world is left observably equivalent. Add fault-injection tests for each snapshot read class. |
| F257-C | HIGH data-loss / crash | Reset is JS-memory snapshot + sequential independent delete transactions, not a true multi-store IDB transaction. | `resetCurrentWorld` (lines 699..786) reads into a JS object via `Promise.all`, then issues `await this.metadata.deleteMetadata(...)`, `await this.metadata.deleteRaw(...)`, `await this.chunkSections.deleteColumn(...)`, `await this.chunkEdits.deleteChunkEdits(...)`, `await this.playerStates.deletePlayerState(...)`, `await this.blockEntities.deleteChunkEntities(...)`, `await this.entities.deleteChunkEntities(...)` in sequence. A tab close / crash / process termination / `requestAnimationFrame` interruption between deletes leaves partial deletion; even on the happy path, a later delete failure triggers a JS rollback that itself is sequential and can itself fail. | One real `readwrite` IndexedDB transaction spanning all six world-owned stores (`world-metadata`, `chunk-sections`, `chunk-edits`, `player-state`, `block-entities`, `entities`); within that transaction issue every delete; rely on IndexedDB atomic commit/abort. The JS rollback may remain as historical coverage but is no longer authoritative. Add fault-injection tests proving transaction abort leaves the world byte/record-equivalent. |
| F257-D | MEDIUM/HIGH data integrity | `WorldArchiver.importWorld` does not validate `metadata.worldId === archive.worldId`. | When `archive.metadata` is present, `importWorld` writes `valid.metadata` directly with no ownership check; columns/entities/etc. are written under `archive.worldId` (the top-level id). A tampered archive can mutate another world's metadata. The player-state `worldId` is silently normalized to the archive's `worldId`, which masks the same class of tampering. | `validateWorldArchive` MUST reject an internally inconsistent archive before any write. Re-evaluate player-state normalization; safer default is rejection. Add tests for tampered `metadata.worldId`, tampered `playerState.worldId`, and any other ownership-bearing payload. |
| F257-E | HIGH cert integrity | Mandatory e2e migration test is `test.skip`. | `tests/e2e/persistence.spec.ts:399` — `test.skip("migrated legacy save restores edits and player state non-destructively", ...)`. Task 44 explicitly requires this. The current skip comment names a "recovery vs ready race". | Investigate the root cause deterministically; fix production code; remove the skip; prove stability by repeated runs. |
| F257-F | HIGH cert integrity | Mandatory e2e pagehide test is `test.skip`. | `tests/e2e/persistence.spec.ts:501` — `test.skip("abrupt-close pagehide flush persists the placed block", ...)`. Task 44 explicitly requires this. The current skip comment names a "loading not hidden" lifecycle issue. | Investigate the `pagehide` / visibility / `AutosaveCoordinator` / dirty-queue / game-disposal / reload lifecycle; fix the production code; remove the skip; prove the edit is durably present after a realistic abrupt close. |
| F257-G | MEDIUM/HIGH governance | Risk-register R-7 was silently removed. | The pre-257 risk register had R-7 (ChunkPipeline dequeued-job / in-flight-stage recovery depending on World rescan). The current `risk-register.md` jumps from R-6 to R-8. Task 53 explicitly required revalidating R-7 and R-8. | Either restore R-7 with current severity, rationale, evidence, and revisit trigger, OR document the source change that resolved it, the tests proving recovery, and why no residual risk remains. Do not silently delete a numbered risk entry; record the resolution in the risk history. |
| F257-H | MEDIUM/HIGH cert integrity | File-audit manifest is structurally complete but semantically vacuous. | `openspec/hardening/2026-08-23-exhaustive-repository-certification/file-audit-manifest.json` has 2642 rows; every row has `runtimeReachability: "unknown"`, `testEvidence: []`, `findingIds: []`, `gitBlob: null`, `purpose: "257 repair coverage for ..."`, `reviewNotes: "auto-reviewed for 257 repair; no findings"`. Even rows labelled `semantic` use this pattern. This is a mechanical inventory, not a human-equivalent semantic review. | For the production files actually touched in the 257 repair (persistence/recovery path, every test file used for certification, every OpenSpec/state/risk file relied upon), populate meaningful `purpose`, `runtimeReachability`, `imports`/`importedBy`, `testEvidence`, `riskAreas`, `findings`, `disposition`, `reviewNotes`, and (if the schema supports) `gitBlob` from the current tree. Distinguish inventory-style auto-review from genuine semantic review. Update validator/policy so the words match what is enforced. Reissue the reviewed manifest at the final candidate SHA. |
| F257-I | HIGH cert integrity | State was previously certifying an older SHA. | The previous `PROGRAM_STATE.json` claimed `published_head: b38d55c`; `origin/main` is now `c9c91dc` with two `test.skip` commits (`5becf0e`, `c9c91dc`). A green CI on `b38d55c` is not green CI on the published `main`. | At every checkpoint record `session_start_head`, implementation candidate SHA, verification evidence SHA, publication SHA, and CI run ID. After push, refetch `origin/main` and update `published_head`. If a state-file follow-up commit is needed, document the relationship explicitly. |
| F257-J | MEDIUM cert integrity | Visual clipped tolerance was raised without documented noise evidence. | `tests/e2e/visual-regression.spec.ts` clipped `maxChangedFraction` raised 0.015 -> 0.02. The commit message and §C247 maintenance fix record environment-day/high/1920x1080 boundary noise ~0.0105. The 0.02 value is defensible but the evidence must be retained, and any misleading "stricter" comment must be fixed. | Collect repeated same-build same-fixture screenshots under the canonical CI software-rendering path; calculate the noise distribution; compare `maxChangedFraction`; retain the evidence; either justify 0.02 with the data or restore a defensible tighter bound. Fix misleading comments. |
| F257-K | MEDIUM cert integrity | Backup round-trip tests check counts, not actual payload equality. | `RecoveryBackupAndAtomicReset.test.ts` round-trip checks `cols.length === 25`, `edits.length === 2`, `witherData.length === 1`, etc. Column content, edit content, block-entity payload, entity payload, player-state payload, and full metadata equality are not asserted. A bug that mutates column bytes can pass the round-trip test. | Capture a complete pre-export semantic snapshot; after successful reset, import into a clean store, read the complete world state, and compare every field of every record (allowing only timestamp normalization on `metadata.updatedAt` and `metadata.createdAt`). Also verify foreign world unchanged throughout. |
| F257-L | MEDIUM cert integrity | Archive import validates pre-write but writes through multiple independent repository operations. | `WorldArchiver.importWorld` (lines 119..162) calls `await this.metadata.putMetadata(...)`, then a `for` loop over `valid.columns` calling `await this.chunkSections.putColumn(...)`, then loops for blockEntityChunks / entityChunks / chunkEdits / witherData / playerState. Each is its own IndexedDB transaction. A later write failure leaves a partial import. | If the normative spec says malformed validation only needs pre-write atomicity, preserve that and add tests asserting partial-import behavior is acceptable. If atomic import is the contract, use the same multi-store transaction layer that reset uses. Add write-failure injection tests proving abort-on-any-failure semantics. |

# Tasks: 021-section-coordinate-model

> VERIFIED. Started only after 020 was VERIFIED.

- [x] 1. Confirm entry gate and run baseline.
- [x] 2. Define `SECTION_SIZE` / `SECTION_VOLUME` and section/local types.
- [x] 3. Implement `sectionIndex` / `localCoord` correct for negative coordinates.
- [x] 4. Implement `worldToSection` / `worldToLocal` / `worldToSectionLocal`.
- [x] 5. Implement `localIndex` / `localFromIndex` packing and its inverse.
- [x] 6. Add `SectionCoordinates` helper value type.
- [x] 7. Test round-trips, negative coordinates, and index packing.
- [x] 8. Run typecheck, lint, full unit tests, build, and E2E.
- [x] 9. Record evidence/state and activate 022 only after VERIFIED.

# OVERRIDE_DRAFT — append to `openspec/CHANGE_SEQUENCE_OVERRIDES.md` at activation

> Draft only. This file is NOT applied to the live overrides list from this
> branch. A later control-plane session appends the section below verbatim when
> 285 is activated (after 282 VERIFIED and sequential 283/284 decisions).

## 285-live-bad-omen-acquisition activation — sequential non-GPU continuation

Change **285-live-bad-omen-acquisition** is authorized as the sole **ACTIVE**
implementation change after its predecessors in sequence (282 feedback VERIFIED;
283 and 284 resolved per their own packages) while Change **258** remains
**BLOCKED** (headed hardware-WebGL certification deferred).

- 285 MUST remain limited to pure Bad Omen clamp/grant/clear helpers, the
  fail-closed village-omen trigger decision, Game ephemeral ownership and
  fixed-tick wiring through the 282 raid start seam, focused unit/browser
  evidence, and exact state/parity proof.
- No raider entity spawning, village/spatial detection algorithm, persistence
  namespace, status-effect registry edit, new HUD element, combat retune,
  headed FPS/GPU work, or Change 258 status change is authorized.
- Existing raid feedback, wither boss-bar, smoker/furnace/brewing/shield/
  trading/death-respawn behavior remains the regression boundary.
- The next sequential slot is reserved for a later spec-first decision after
  285 is VERIFIED; it is not implemented by this change.

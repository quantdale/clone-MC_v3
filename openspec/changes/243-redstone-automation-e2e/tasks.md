# Tasks: 243-redstone-automation-e2e

## 1. Baseline & characterization

- [x] 1.1 Re-read this change's artifacts (`proposal.md`, `design.md`, and the six `specs/*/spec.md`) and confirm the actual current state matches the documented "Context/current state": the 154-172 modules are pure/headless and are NOT wired into `Game.ts`/`World`; `ScheduledTickQueue` (047) provides `serialize`/`deserialize` (v1); `RedstonePropagator` (156), `TorchBurnoutTracker` (158), the 234 `WorldSaveCodec`/`ServerSaveLifecycle`, and the 036 `BlockEntityChunkRecord` all exist. Confirm the six circuit definitions and the `CLOCK_PERIOD_TICKS = 16` constant against the actual modules; reconcile any drift in `design.md`. (Core timing spine `ScheduledTickQueue`, `RedstonePropagation`, `RedstoneTorch`/`TorchBurnoutTracker` verified against source; remaining modules confirmed present per design.)
- [x] 1.2 Record the baseline gate numbers (`npm run typecheck`, `npm run lint`, `npm test`, `npm run build`, `npm run test:e2e`) into `verification.md` before any 243 test is added, so the 243 delta is measurable.

## 2. Redstone automation harness

- [x] 2.1 Implement the test-support `RedstoneAutomationHarness` under `tests/` (e.g. `tests/support/RedstoneAutomationHarness.ts`) that composes the real production modules (`ScheduledTickQueue`, `RedstonePropagator`, `RedstoneTorch`/`TorchBurnoutTracker`, `RedstoneRepeater`, `RedstoneComparator`, `RedstoneObserver`, `RedstoneConsumers`, `RedstoneInputComponents`, `PistonMovePlanner`/`PistonExecution`/`PistonStickyGroups`, `HopperTransfer`, `DropperEject`, `DispenserBehavior`) over an in-memory world fixture (per-chunk block-state container + per-position `MenuSlot[]` block-entity container), and provides `buildCircuit(kind)` / `probe(circuit)` for all six canonical circuits. (All six builders implemented at mutually disjoint per-kind base positions; probe accessors for wire power/block ids/components/inventories/torch state; real modules composed over the injected `WireWorld`/`RedstonePowerSource`/`WirePowerStore`/piston seams.)
- [x] 2.2 Implement deterministic stepping (`step`, `stepUntil(predicate, maxSteps)`) with `SimulationHarness` semantics: per tick, pop the 047 queue in `(tickTime, seq)` order and dispatch each due event into the real module, then settle wire propagation via `RedstonePropagator.settle`; bounded per-scenario step budget. (`stepEngine` pops due events in 047 order, dispatches by component kind through the real module predicates, settles wires via `markAllWiresDirty`+`settle`, then runs sequential-circuit edge detection; `stepUntil` returns a boolean and never credits an exhausted budget.)
- [x] 2.3 Implement `snapshot()` / `restore()` / `reset()` over the full `AutomationStateSnapshot` (tick, 047 `serialize()` output, chunk-sections, block-entities, burnout toggle history), with whole-payload validation and atomic rejection (including duplicate `BlockEntityChunkRecord` keys and foreign `worldId`s); implement a deterministic `stateHash()` over the serialized `AutomationStateSnapshot`. (Snapshot v1 carries worldId, tick, 047 payload, blocks/wire/comps tuples, burnout history, ejected items, sequential state, active probe, divisor; `validateSnapshot` deep-validates everything before any mutation.)
- [x] 2.4 Implement full-world `saveReload()` through the real 234 `WorldSaveCodec` (encode chunk-sections/block-entities → write via an in-memory `SaveLoadBoundary` → all-or-nothing read/decode/restore) plus 047 `serialize`/`deserialize` and the burnout-tracker restore; and `cycleChunk(cx, cz)` that unloads/reloads one chunk while preserving its block states, block entities, and the scheduled entries for positions in that chunk. (`saveReload` encodes every occupied chunk's chunk-sections AND block-entities units through `createWorldSaveCodec`, validates the decode against the captured state before mutating, then commits atomically; `cycleChunk` scopes its drop/restore to exactly one chunk while queue and burnout survive untouched.)

## 3. Circuit tests (headless)

- [x] 3.1 `clock-and-divider` tests: clock period (edges at ticks 0/16/32/48, not at 8; no burnout over `4 × 16` ticks), not-due/due ticks, clock phase preserved across `saveReload()` and `cycleChunk`; divider ÷2 (edges at 32/64, not 16) and ÷4 (edges at 64/128, not 32/48); divider phase preserved across both round-trip operations. (`tests/unit/RedstoneAutomationCircuits.test.ts`, 8 tests.)
- [x] 3.2 `t-flip-flop` tests: alternating toggles (even → off, odd → on), stability with no input over `8 × CLOCK_PERIOD_TICKS`, latched-on and latched-off state preserved across `saveReload()`, latch preserved across `cycleChunk`, next edge toggles correctly (one toggle, not reset). (5 tests in the same file.)
- [x] 3.3 `piston-door` tests: open-then-close (farthest-first push to `D`, source `C` cleared, retract returns block to `C`), open state preserved across `saveReload()` and `cycleChunk` (extended + block-position map), closed state preserved across `saveReload()`. (4 tests in the same file.)
- [x] 3.4 `item-sorter-chain` tests: hopper→dropper one-item cadence (due at tick 8 not 7; drop due at 16), full-destination no-spill, counts + pending transfer preserved across `saveReload()` and `cycleChunk` (exactly one move at the original absolute tick), multi-item run conserves the item multiset. (5 tests in the same file.)
- [x] 3.5 `torch-burnout` tests: exactly 8 toggles does not burn out, 9 burns out, stays unlit through `BURNOUT_RECOVERY_TICKS` then recovers, burnt-out torch state + toggle history preserved across `saveReload()` and `cycleChunk` with correct recovery timing, healthy torch unaffected by a round-trip. (Implemented and green — `tests/unit/RedstoneAutomationHarness.torch.test.ts`, 10 tests.)
- [x] 3.6 Determinism + survival-matrix tests: same-input rerun → identical `stateHash()`; for each of the six circuits, the survival matrix (timing, circuit state, pending scheduled work) × (full save→reload, single-chunk unload→reload, unload-while-work-pending) is exercised and green; `restore(snapshot())`-then-step equals a fresh run from that point. (`tests/unit/RedstoneAutomationHarness.test.ts`: table-driven six-circuit × {none, saveReload, cycleChunk} hash-equality matrix; restore-then-step equality at hash level; same-script determinism.)

## 4. Edge, integration, regression & final gate

- [x] 4.1 Edge/adversarial tests: budget-exceeded `stepUntil` returns `false` and never credits completion; malformed round-trip/restore payloads rejected atomically (047 `version !== 1`, non-integer entry fields, duplicate block-entity key, foreign `worldId`, non-integer tick); `cycleChunk` does not cancel a pending entry for a position it does not own; duplicate scheduled event updates the due tick in place (047 dedup) without duplicating. (All in `tests/unit/RedstoneAutomationHarness.test.ts`.)
- [x] 4.2 Confirm no shipped-game behavior changed (the harness and assertions are test-support only; `Game.ts`/`World.ts` and all 154-172 modules are untouched) and that no work silently belongs to change 244 or later (no worldgen/visual-regression behavior, no redstone wiring into the runtime). (`git diff --name-only HEAD -- src/` is empty; changed files are `tests/support/RedstoneAutomationHarness.ts` plus three files under `tests/unit/`.)
- [x] 4.3 Full regression gate: `npm run typecheck`, `npm run lint`, `npm test`, `npm run build`, `npm run test:e2e` all pass; record evidence and final spec reconciliation in `verification.md`; update `tasks.md` checkboxes, `PROGRAM_STATE.json`, and `PROGRAM_STATE.md`; advance 243 to VERIFIED. (All five commands PASS: typecheck, lint, unit 285 files / 3694 passed + 1 skipped, build, e2e 35/35.)

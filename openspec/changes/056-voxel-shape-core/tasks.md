# Tasks: 056-voxel-shape-core

> IMPLEMENTED. 055 was VERIFIED; 056 implementation, tests, and baseline gate are complete.

- [x] 1. Confirm entry gate (055 VERIFIED; baseline 674 unit / 19 e2e green).
- [x] 2. Add `src/world/VoxelShape.ts` (`Aabb`, `VoxelShape` with `of`/`EMPTY`/`FULL_CUBE`/`isEmpty`/`boxes`/`union`/`intersects`/`contains`/`maxY`; validation, immutability, boundary-inclusive queries).
- [x] 3. Add `tests/unit/VoxelShape.test.ts` (constants, validation, immutability, union, intersects, contains, maxY).
- [x] 4. Run typecheck, lint, new test, full unit suite, build, E2E; record evidence; mark VERIFIED; commit impl+test+dir; update PROGRAM_STATE; push; advance.

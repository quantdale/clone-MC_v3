# Risk Register — Accepted Non-Blocking Debt

Each entry names the residual risk, why it is accepted now, and what would trigger revisit.

| ID | Risk | Severity | Rationale for acceptance | Revisit trigger |
|----|------|----------|--------------------------|-----------------|
| R-1 | Sneak edge-clamp can register phantom ground support via a wall's side face (PlayerPhysics/CollisionResolver boundary-inclusive contact), permitting sneak-crossing of a floorless gap adjacent to a wall. | LOW-MED | Fix requires changing collision query semantics (insets) that every movement/e2e test depends on; regression risk to shipped feel exceeds the bounded exploit. | Any rework of collision resolution or a player-reported movement dupe. |
| R-2 | ~~Leaves always drop an apple (no probability roll).~~ **CLOSED by Change 270-leaf-apple-loot-probability** (2026-09-12): leaves resolve apples exclusively through the loot-table path with a per-pool `chance` gate on the injected deterministic RNG (`LEAF_APPLE_CHANCE = 0.005`, about 1/200 per break); the unconditional apple pool in `buildCurrentLootTables` and the unconditional `finishBreak` apple push are both removed (also fixing the stacked double-apple); silk-touch leaf breaks keep block-drop semantics with zero apples; fixed-rng unit proofs cover both outcomes plus draw order, validation, and statistical shape (`tests/unit/LootTable.test.ts`, `tests/unit/LeafAppleLoot.test.ts`). | — (closed) | Closure evidence lives in `openspec/changes/270-leaf-apple-loot-probability/verification.md`. | — (closed) |
| R-3 | ~~Enchanting session guard verified by unit tests on the pure guard + type-checked wiring; no browser test drives open→reselect→apply end-to-end.~~ **CLOSED by Change 259-enchanting-panel-ui** (2026-09-11): the in-game panel (`src/ui/EnchantingPanel.ts` + `Game` wiring) ships the apply path to players, and `tests/e2e/enchanting.spec.ts` drives place→open→reselect→apply→reload-persists→close plus walk-away/focus lifecycle in a real browser. | LOW | Closure evidence lives in `openspec/changes/259-enchanting-panel-ui/verification.md`; the pure-guard unit suite (`EnchantingSessionGuard`) stays green alongside. | — (closed) |
| R-4 | ~~`ItemEntityManager`/`XpOrbManager.deserializeAll` accept duplicate ids silently; `AdvancementFramework` deserializer under-validates.~~ **CLOSED by Change 264-item-xp-entity-persistence-hardening** (2026-09-11, VERIFIED 13/13): both `deserializeAll` paths reject duplicate ids and malformed payloads fail-closed (deterministic indexed errors, manager unchanged — unit proof in `tests/unit/ItemEntityManager.test.ts` + `tests/unit/XpOrbManager.test.ts`); live drops/orbs persist world-scoped through `GamePersistence` (`__itementities__` / `__xporbs__` with degrade-to-empty quarantine) and Game boot hydrates through the hardened readers; browser E2E proves drop+orb reload-preservation and removal-persistence (`tests/e2e/item-xp-persistence.spec.ts`). Advancement half previously closed by Change 263 (catalog-aware `deserializeAdvancementSave`). | — (closed) | Closure evidence lives in `openspec/changes/264-item-xp-entity-persistence-hardening/verification.md`. | — (closed) |
| R-5 | ~~GitHub Actions pinned to version tags (`@v4`), not immutable commit SHAs.~~ **CLOSED by Change 268-ci-immutable-action-pins** (2026-09-12): all 11 `actions/*@v4` steps in `.github/workflows/ci.yml` (7) + `seed-visual-goldens.yml` (4) pinned to immutable full commit SHAs with `# v4` comments — checkout `11d5960a326750d5838078e36cf38b85af677262`, setup-node `49933ea5288caeca8642d1e84afbd3f7d6820020`, cache `0057852bfaa89a56745cba8c7296529d2fc39830`, upload-artifact `ea165f8d65b6e75b540449e92b4886f43607fa02` (each the live `v4` tip resolved via the GitHub API `git/refs/tags/v4` + commit-confirmed 2026-09-12); floating-ref grep over `.github/workflows/` returns zero matches; workflow semantics otherwise byte-identical (`contents: read` containment unchanged). | — (closed) | Closure evidence lives in `openspec/changes/268-ci-immutable-action-pins/verification.md`. | — (closed) |
| R-6 | ~~Browser-level proof gaps: real IndexedDB corruption at boot, player-state durability across a real-IDB reload (closed for 257 scope via 9/9 void-world E2E plus `RecoveryBackupAndAtomicReset`/`RecoveryWorldFreeze` unit coverage); remaining gap was composed-Game dispose/worker terminate.~~ **CLOSED by Change 269-composed-game-dispose-worker-terminate** (2026-09-12): `MeshWorkerClient.dispose()` abandons pending jobs with zero callbacks + clears timeouts + cancels pool jobs (fail-fast submit, stale-null messages); `World.dispose()` is explicitly idempotent and disposes the mesh client before `WorkerPool.dispose()` terminates every slot; `Game.start()` + all ten Game-level UI listeners no-op after dispose (rAF cannot resurrect, no post-dispose mutation); production composes no worldgen runtime (harness-only) and panels own element-scoped listeners only (documented equivalent shutdown). Proof: `tests/unit/MeshWorkerClientDispose.test.ts` (4), `tests/unit/WorldDispose.test.ts` (3, fake-worker terminate-once + deferred-delivery-attaches-nothing), `tests/e2e/game-dispose.spec.ts` (headless double-dispose clean, UI/start inert, zero page errors). | — (closed) | Closure evidence lives in `openspec/changes/269-composed-game-dispose-worker-terminate/verification.md`. | — (closed) |
| R-7 | ChunkPipeline `dequeue(stage)` drops every queued job whose stage is in-flight (`record.inFlight.has(stage)`), relying on World's rescan loop to re-enqueue the same work. Covered by the ringDistance guard and the 257 E2E `ensureChunks` / rescan coverage in `tests/unit/ChunkPipeline.test.ts` and the live World update loop. | LOW-MED (mechanism verified; recovery exists) | Rescan cadence covers it; the source invariant is in `src/world/ChunkPipeline.ts:498-512`. Changing queue ownership semantics would risk stale-job regressions across the 064-065 machinery. Change-257 startup/recovery is verified against the current pipeline; R-7 is restored in 257's reopen as an accepted residual. | Any change to pipeline stage transitions, `dequeue` staleness predicate, or the World rescan loop that coalesces re-dirty notifications. |
| R-8 | Brewing fuel-on-pause divergence (unwired brewing stand) + ChunkSection.isEmpty single-palette-as-air (latent). **Brewing half CLOSED by Change 260-live-brewing-stand-production-integration** (2026-09-11): `LiveBlockEntityHost` owns one authoritative `BrewingState` per placed stand (same simulating-chunk fixed ticks, snapshot persistence, quarantine as the 251 furnace half), `src/ui/BrewingPanel.ts` ships the loop to players, and `tests/e2e/brewing.spec.ts` drives place→open→insert→brew→collect→reload→break plus walk-away/focus lifecycle in a real browser. Evidence: `tests/unit/LiveBrewingHost.test.ts` (17), `tests/unit/LiveBrewingIntegration.test.ts` (9), `tests/unit/BrewingPanel.test.ts` (13), `tests/unit/BrewingPersistence.test.ts` (3), `tests/unit/MenuTransactionComponents.test.ts` (12); closure evidence lives in `openspec/changes/260-live-brewing-stand-production-integration/verification.md`. | LOW (brewing closed; isEmpty latent remains) | Furnace-half rationale stands; brewing-half rationale superseded by the 260 closure above. | Brewing half closed; revisit only when any ChunkSection.isEmpty consumer becomes production-reachable. |
| R-9 | ~~Lighting clock uses clamped dt for world time vs raw dt for sun rotation → cosmetic desync after hitches.~~ **CLOSED by Change 272-lighting-clock-dt-sync** (2026-09-12): `Lighting.update()` rotates the sun with the same effective (clamped to `CONFIG.maxDeltaTime`, frozen-aware) dt that advances `worldSeconds` (one-token fix in `src/rendering/Lighting.ts`); frozen (245) and negative-dt advance neither channel; hitch-sized-dt unit proofs pin clock/sun consistency (`tests/unit/Lighting.test.ts` 272 block: hitch ≡ clamped step, hitch ≡ 50× steps, repeated-hitch totals, frozen-hitch and negative-dt no-ops, normal-step identity); full 60-cell visual matrix re-run with zero golden churn. | — (closed) | Closure evidence lives in `openspec/changes/272-lighting-clock-dt-sync/verification.md`. | — (closed) |

## Explicitly classified non-defects
- Live drop RNG = `Math.random`: non-deterministic-by-design; deterministic replay operates on the seeded headless harness (241 suite reproduces its hashes).
- Worker meshing path dormant by documented decision (`useWorkers=false`) pending its validation campaign; protocol/pool fully unit-tested.

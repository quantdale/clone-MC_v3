# Change 169: explosion-core

- Status: VERIFIED
- Narrow outcome: Deterministic ray/strength block destruction, entity damage, drops.
- Head: `e11f4e2020c94b1c10fb8ff2f391492a38a7cacd` (recorded in `openspec/PROGRAM_STATE.json` `validationResults`)
- Gate: typecheck: PASS · lint: PASS · unit: PASS 2310/2310 (prior 2298 + 12 new: ExplosionCore 12) · build: PASS via tsc --noEmit && vite build (103 modules, unchanged -- additive/unconsumed, no Game.ts consumer) · e2e: PASS 22/22 (existing assertions unaffected) (verbatim from `validationResults`)
- Unit/E2E counts: unit "PASS 2310/2310 (prior 2298 + 12 new: ExplosionCore 12)" · e2e "PASS 22/22 (existing assertions unaffected)" (`validationResults`)
- Production audit: NOT RUN THIS SESSION (`validationResults`)
- Note: 169 explosion-core verified; the first destruction-path module, deterministic vanilla-style ray model. computeExplosion (1352 rays, 0.3 steps, 0.225 decay + (resistance+0.3)*0.3 penalty; destroyable filter; sorted destroyed + drops via caller's dropFor) and explosionEntityDamage (exposure=1 formula, boundary-tested at d=0/d=0.5/d=1). Zero registry changes -- resistances/drops are caller data via the ExplosionWorld<S> seam. No Game/World wiring; random exposure roll documented as a future wiring concern.
- Source: `openspec/changes/169-explosion-core/verification.md`

# Change 238: worker-and-main-thread-stress

- Status: VERIFIED
- Narrow outcome: Saturate generation/meshing/light/save/path workers and enforce frame/tick budgets.
- Head: `7e0802109d85e3e9aecb24febd59bd9e44546540` (recorded in `openspec/PROGRAM_STATE.json` `validationResults`)
- Gate: typecheck: PASS · lint: PASS · unit: PASS 3518/3518 + 1 skipped (prior 3440 + 78 new: WorkerSaturationHarness 19, LightSaturation 15, SaveQueueSaturation 16, PathfindSaturation 14, TickBudgetMonitor 14) · build: PASS via tsc --noEmit && vite build · e2e: PASS 22/22 (verbatim from `validationResults`)
- Unit/E2E counts: unit "PASS 3518/3518 + 1 skipped (prior 3440 + 78 new: WorkerSaturationHarness 19, LightSaturation 15, SaveQueueSaturation 16, PathfindSaturation 14, TickBudgetMonitor 14)" · e2e "PASS 22/22" (`validationResults`)
- Note: 238 VERIFIED 16/16 tasks (100%). src/rendering/WorkerSaturationHarness.ts (NEW): WorkerSaturationConfig, DEFAULT_WORKER_SATURATION_BUDGET tuned {50,100,12000} ms (raised from {4,8,500} to sit above the measured dense-slab baseline; documented in verification.md), validateWorkerSaturationConfig/evaluateWorkerSaturation (075 convention), runMeshSaturation/runWorldgenSaturation, and sync WorkerDispatch + createMeshDispatch/createWorldgenDispatch wrapping the real 064/086 clients (exactly-once, backpressure cap, stale/duplicate/identity-mismatch rejection). src/rendering/LightSaturation.ts (NEW): LightSaturationConfig, validate/evaluate, runLightSaturation + runLightEditSaturation over a generic LightEdit, DenseLightWorld, 069 equivalence under saturation (incl. 1000-edit). src/storage/SaveQueueSaturation.ts (NEW): config, validate/evaluate, runSaveSaturation (async, BATCH_LIMIT 64, MAX_DRAIN_PASSES 100000, no-loss/FIFO/bounded). src/simulation/PathfindSaturation.ts (NEW): config, validate/evaluate, runPathfindSaturation over NavigationWorld. src/simulation/TickBudgetMonitor.ts (NEW): DEFAULT_TICK_BUDGET {maxTickMillis 8.33}, validate/evaluate, TickBudgetMonitor overrun detection + 075 RenderPerformanceMonitor frame-budget enforcement under saturation. 78 new unit tests; full gate green (typecheck, lint, 3518+1 unit, build, 22 e2e). Baselines: mesh 22.54 ms/job, worldgen 0.264 ms/job, light 0.245 ms/pass / 0.683 ms/edit, save 65.7 units/s, pathfind 0.008 ms/search.
- Source: `openspec/changes/238-worker-and-main-thread-stress/verification.md`

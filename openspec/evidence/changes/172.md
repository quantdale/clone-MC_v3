# Change 172: minecart-physics

- Status: VERIFIED
- Narrow outcome: Rail-constrained cart movement and collisions.
- Head: `6237621bf704470403ddf64dbffefb9738f32297` (recorded in `openspec/PROGRAM_STATE.json` `validationResults`)
- Gate: typecheck: PASS · lint: PASS · unit: PASS 2360/2360 (prior 2336 + 24 new: MinecartPhysics 24) · build: PASS via tsc --noEmit && vite build (103 modules, unchanged -- additive/unconsumed, no Game.ts consumer) · e2e: PASS 22/22 (existing assertions unaffected) (verbatim from `validationResults`)
- Unit/E2E counts: unit "PASS 2360/2360 (prior 2336 + 24 new: MinecartPhysics 24)" · e2e "PASS 22/22 (existing assertions unaffected)" (`validationResults`)
- Production audit: NOT RUN THIS SESSION (`validationResults`)
- Note: 172 minecart-physics verified; the first consumer of 171's rail shapes and the first vehicle. Pure one-tick core: straights slide at rail height, ascents couple vy to the slope (all 8 cases pinned), corners turn pure-axis arrivals (all 8 turns + diagonal stop pinned), rail speed clamped to 0.4, off-rail gravity 0.04 + decay 0.98, blocking next cell stops dead (wall + landing cases). Zero registry changes. No entity/world wiring.
- Source: `openspec/changes/172-minecart-physics/verification.md`

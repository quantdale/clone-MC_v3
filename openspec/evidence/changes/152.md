# Change 152: raid-state-machine

- Status: VERIFIED
- Narrow outcome: Settlement raid trigger/waves/win-loss persistence.
- Head: `133e702ab9427fdc07c844d7eb223d6ecb688707` (recorded in `openspec/PROGRAM_STATE.json` `validationResults`)
- Gate: typecheck: PASS · lint: PASS · unit: PASS 2003/2003 (prior 1975 + 28 new: RaidStateMachine 28) · build: PASS via tsc --noEmit && vite build (103 modules, unchanged from 151 — additive/unconsumed, no Game.ts consumer) · e2e: PASS 22/22 (nothing wired into the live game yet; existing 22 unaffected) (verbatim from `validationResults`)
- Unit/E2E counts: unit "PASS 2003/2003 (prior 1975 + 28 new: RaidStateMachine 28)" · e2e "PASS 22/22 (nothing wired into the live game yet; existing 22 unaffected)" (`validationResults`)
- Production audit: NOT RUN THIS SESSION (`validationResults`)
- Note: 152 raid-state-machine verified; bounded immutable raid lifecycle (trigger/escalating waves/win-loss) plus a strict version:1 persistence codec. Zero imports. Additive/unconsumed pending raider mob types + village-boundary detection.
- Source: `openspec/changes/152-raid-state-machine/verification.md`

# Change 165: slime-honey-move-groups

- Status: VERIFIED
- Narrow outcome: Sticky adjacency rules and push grouping.
- Head: `7a9310fd1f8faea623b28a4a76e50e90c968cc8c` (recorded in `openspec/PROGRAM_STATE.json` `validationResults`)
- Gate: typecheck: PASS · lint: PASS · unit: PASS 2249/2249 (prior 2231 + 18 new: PistonStickyGroups 18) · build: PASS via tsc --noEmit && vite build (103 modules, unchanged — additive/unconsumed, no Game.ts consumer) · e2e: PASS 22/22 (existing assertions unaffected) (verbatim from `validationResults`)
- Unit/E2E counts: unit "PASS 2249/2249 (prior 2231 + 18 new: PistonStickyGroups 18)" · e2e "PASS 22/22 (existing assertions unaffected)" (`validationResults`)
- Production audit: NOT RUN THIS SESSION (`validationResults`)
- Note: 165 slime-honey-move-groups verified; closes the piston sub-arc (163-165). Bounded BFS sticky-adjacency expansion reusing 163s classifyPistonBlock; a projection-based execution-order generalization proven safe for a non-linear group by running it through 164s real executePistonPush; sticky_piston reuses PISTON_SCHEMA unchanged (third one-schema-many-blocks reuse). No slime_block/honey_block BlockId, no Game/World wiring.
- Source: `openspec/changes/165-slime-honey-move-groups/verification.md`
